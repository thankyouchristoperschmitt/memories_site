# Christopher Schmitt Memories site

## Install
- Clone repository
- Install Eleventy using npm `npm install -g @11ty/eleventy` in the command line.
- Run site locally, using `eleventy --serve`, at `localhost:8080`
